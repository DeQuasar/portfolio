OPENAI_API_KEY=
OPEN_API_KEY=
# Absolute directory containing your résumé PDF. Used by docker-compose volume mounts.
HOST_RESUME_SOURCE_DIR=/mnt/c/Users/your-user/OneDrive/Desktop/Resumes
# Optional explicit résumé path for local tooling (overrides the directory search).
RESUME_SOURCE_PATH=
# Optional directory to search for résumé files when running scripts directly.
RESUME_SOURCE_DIR=
